# Copy this file to ".env" and adjust values as needed.

# Minimum ISO datetime for included posts.
# Example: 2024-01-01T00:00:00.000Z
MIN_PUBLISHED_DATE=

# Logging verbosity: error, warn, info, debug
LOG_LEVEL=info

# Optional: proxies for external HTTP clients (if you add them later)
HTTP_PROXY=
HTTPS_PROXY=

# Optional: platform credentials (not used directly in this sample implementation,
# but reserved for real API clients).
YOUTUBE_API_KEY=
INSTAGRAM_ACCESS_TOKEN=
TIKTOK_ACCESS_TOKEN=